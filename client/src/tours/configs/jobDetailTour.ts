import type { TourConfig } from '../types';

export const jobDetailTour: TourConfig = {
  id: 'job-detail',
  // TODO: Tour steps will be re-added once data-tour attributes are placed in the new tab layout
  steps: [],
};
